ADF pipeline / dataset / linked-service JSON gets exported here once the factory is
provisioned and pipelines are built (Copy activities for the CSV drop, the SQL DB
incremental copy, and the REST/Socrata API pull).
